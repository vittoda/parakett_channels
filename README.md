# FlowStack Channels

Channels provide inputs to the FlowStack agents. Inputs from the channels trigger events to the agent. Unlike MCP servers in FlowStack, channels are part of the FlowStack server process.

*channel_base* provides common features required for all channels. Hence build this module before building individual channels
